{% extends 'base.html.twig' %}

{% block title %}Edit Homepage{% endblock %}

{% block body %}
    <h1>Edit Homepage</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}
    <a href="{{ path('home') }}" class="btn btn-secondary mt-3">Back to list</a>
{% endblock %}
